<!DOCTYPE html>
<html lang="es" class="dark scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MIRADAS — Nacho Pedrós</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Google Fonts: Cormorant Garamond & Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Inter:wght@200;300;400;500&display=swap" rel="stylesheet">

  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            brand: {
              black: '#09090b',
              card: '#121215',
              border: '#27272a',
              gold: '#d4af37',
              goldHover: '#f3e5ab',
              muted: '#8e8e93'
            }
          },
          fontFamily: {
            serif: ['Cormorant Garamond', 'serif'],
            sans: ['Inter', 'sans-serif']
          }
        }
      }
    }
  </script>

  <style>
    body {
      background-color: #09090b;
      color: #f4f4f5;
      font-family: 'Inter', sans-serif;
    }
    .serif {
      font-family: 'Cormorant Garamond', serif;
    }
    .image-zoom-container {
      overflow: hidden;
    }
    .image-zoom-container img {
      transition: transform 0.7s cubic-bezier(0.25, 1, 0.5, 1);
    }
    .image-zoom-container:hover img {
      transform: scale(1.05);
    }
    #menu-overlay {
      transition: opacity 0.3s ease-in-out;
    }
  </style>
</head>
<body class="bg-brand-black text-zinc-100 antialiased selection:bg-brand-gold selection:text-black">

  <!-- TOP NAVIGATION HEADER -->
  <header class="fixed top-0 left-0 w-full z-40 bg-brand-black/80 backdrop-blur-md border-b border-brand-border">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <a href="#hero" class="text-2xl tracking-widest font-serif font-light text-zinc-100 uppercase hover:text-brand-gold transition-colors">
        MIRADAS
      </a>
      
      <div class="flex items-center space-x-6">
        <span class="hidden md:inline-block text-xs uppercase tracking-widest text-brand-muted">Nacho Pedrós</span>
        
        <!-- HAMBURGER BUTTON -->
        <button id="menu-btn" aria-label="Abrir Menú" class="flex items-center space-x-2 text-zinc-300 hover:text-brand-gold focus:outline-none">
          <span class="text-xs uppercase tracking-widest">Menú</span>
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </header>

  <!-- OVERLAY / SLIDE-OUT MENU DRAWER -->
  <div id="menu-overlay" class="fixed inset-0 bg-black/80 z-50 pointer-events-none opacity-0 transition-opacity duration-300"></div>
  
  <aside id="menu-drawer" class="fixed top-0 right-0 w-full sm:w-96 h-full bg-brand-card z-50 transform translate-x-full transition-transform duration-300 ease-in-out border-l border-brand-border p-8 flex flex-col justify-between">
    <div>
      <div class="flex justify-between items-center mb-12">
        <span class="text-xs tracking-widest uppercase text-brand-gold">Navegación</span>
        <button id="close-btn" aria-label="Cerrar Menú" class="text-zinc-400 hover:text-white text-2xl focus:outline-none">
          &times;
        </button>
      </div>
      
      <nav class="space-y-6">
        <a href="#hero" class="menu-link block text-2xl font-serif text-zinc-300 hover:text-brand-gold transition-colors">Inicio</a>
        <a href="#galeria" class="menu-link block text-2xl font-serif text-zinc-300 hover:text-brand-gold transition-colors">Obras</a>
        <a href="#historia" class="menu-link block text-2xl font-serif text-zinc-300 hover:text-brand-gold transition-colors">Historia</a>
        <a href="#contacto" class="menu-link block text-2xl font-serif text-zinc-300 hover:text-brand-gold transition-colors">Contacto</a>
      </nav>
    </div>

    <div class="border-t border-brand-border pt-6">
      <p class="text-xs uppercase tracking-widest text-brand-muted mb-2">Nacho Pedrós</p>
      <p class="text-xs text-zinc-500">Colección de Obras & Visual Art</p>
    </div>
  </aside>

  <!-- HERO SECTION -->
  <section id="hero" class="min-h-screen pt-24 flex items-center justify-center relative px-6 overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-zinc-800/20 via-brand-black to-brand-black -z-10"></div>
    
    <div class="max-w-4xl text-center space-y-6">
      <span class="text-xs md:text-sm uppercase tracking-[0.3em] text-brand-gold font-light">Exposición Portfolio</span>
      <h1 class="text-5xl md:text-8xl font-serif font-light tracking-wider text-zinc-100">
        MIRADAS
      </h1>
      <p class="text-lg md:text-xl font-light text-brand-muted max-w-2xl mx-auto leading-relaxed">
        Obras de Nacho Pedrós. Una exploración estética a través de luces, claroscuros y texturas profundas.
      </p>
      <div class="pt-8">
        <a href="#galeria" class="inline-block px-8 py-3 border border-brand-gold text-brand-gold hover:bg-brand-gold hover:text-black transition-all duration-300 tracking-widest text-xs uppercase">
          Ver Colección
        </a>
      </div>
    </div>
  </section>

  <!-- GALERÍA / OBRAS SECTION -->
  <section id="galeria" class="py-24 px-6 max-w-7xl mx-auto">
    <div class="text-center max-w-2xl mx-auto mb-16 space-y-3">
      <span class="text-xs uppercase tracking-[0.25em] text-brand-gold">Catálogo</span>
      <h2 class="text-4xl md:text-5xl font-serif font-light">Colección de Obras</h2>
      <div class="w-12 h-[1px] bg-brand-gold mx-auto mt-4"></div>
    </div>

    <!-- FILTROS -->
    <div class="flex flex-wrap justify-center gap-4 mb-12" id="filters">
      <button class="filter-btn active px-4 py-2 text-xs uppercase tracking-widest border border-brand-gold bg-brand-gold text-black transition-all" data-filter="all">Todas</button>
      <button class="filter-btn px-4 py-2 text-xs uppercase tracking-widest border border-brand-border text-brand-muted hover:border-brand-gold hover:text-brand-gold transition-all" data-filter="Retratos">Retratos</button>
      <button class="filter-btn px-4 py-2 text-xs uppercase tracking-widest border border-brand-border text-brand-muted hover:border-brand-gold hover:text-brand-gold transition-all" data-filter="Claroscuro">Claroscuro</button>
      <button class="filter-btn px-4 py-2 text-xs uppercase tracking-widest border border-brand-border text-brand-muted hover:border-brand-gold hover:text-brand-gold transition-all" data-filter="Sacro & Expresión">Sacro & Expresión</button>
    </div>

    <!-- GRID DE OBRAS -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8" id="artwork-grid">
      <!-- Se genera dinámicamente mediante JS -->
    </div>
  </section>

  <!-- HISTORIA SECTION -->
  <section id="historia" class="py-24 px-6 bg-brand-card/40 border-y border-brand-border">
    <div class="max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
      <div class="space-y-6">
        <span class="text-xs uppercase tracking-[0.25em] text-brand-gold">El Artista</span>
        <h2 class="text-4xl md:text-5xl font-serif font-light">Nacho Pedrós</h2>
        <p class="text-zinc-400 font-light leading-relaxed">
          La obra de Nacho Pedrós se articula en torno al estudio del rostro humano, la figura en sombras y el contraste dramático de luces y sombras. 
        </p>
        <p class="text-zinc-400 font-light leading-relaxed">
          En la serie <strong class="text-zinc-200">MIRADAS</strong>, cada pieza rescata la expresividad latente en la penumbra, empleando técnicas de carboncillo, grafito y pigmentos oscuros sobre papel y lienzo.
        </p>
      </div>
      <div class="border border-brand-border p-8 bg-brand-black/60 relative">
        <div class="text-center space-y-4">
          <p class="font-serif italic text-2xl text-zinc-200">"El arte no reproduce lo visible, sino que hace visible lo imperceptible."</p>
          <span class="block text-xs uppercase tracking-widest text-brand-gold">— Nacho Pedrós</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CONTACTO SECTION -->
  <section id="contacto" class="py-24 px-6 max-w-4xl mx-auto">
    <div class="text-center mb-12 space-y-3">
      <span class="text-xs uppercase tracking-[0.25em] text-brand-gold">Información</span>
      <h2 class="text-4xl font-serif font-light">Adquisiciones y Contacto</h2>
      <p class="text-brand-muted text-sm max-w-md mx-auto">Para consultas sobre disponibilidad, encargos o exposiciones, ponte en contacto directo.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12 text-center">
      <div class="p-6 border border-brand-border bg-brand-card/30">
        <h3 class="text-xs uppercase tracking-widest text-brand-gold mb-2">Teléfono</h3>
        <p class="text-zinc-300 font-light">+34 600 000 000</p>
      </div>
      <div class="p-6 border border-brand-border bg-brand-card/30">
        <h3 class="text-xs uppercase tracking-widest text-brand-gold mb-2">Correo</h3>
        <p class="text-zinc-300 font-light">contacto@nachopedros.com</p>
      </div>
      <div class="p-6 border border-brand-border bg-brand-card/30">
        <h3 class="text-xs uppercase tracking-widest text-brand-gold mb-2">Ubicación</h3>
        <p class="text-zinc-300 font-light">España</p>
      </div>
    </div>

    <form id="contact-form" class="space-y-6 max-w-xl mx-auto bg-brand-card/40 p-8 border border-brand-border">
      <div>
        <label class="block text-xs uppercase tracking-widest text-brand-muted mb-2" for="name">Nombre</label>
        <input type="text" id="name" required class="w-full bg-brand-black border border-brand-border text-zinc-100 p-3 text-sm focus:border-brand-gold focus:outline-none">
      </div>
      <div>
        <label class="block text-xs uppercase tracking-widest text-brand-muted mb-2" for="email">Correo Electrónico</label>
        <input type="email" id="email" required class="w-full bg-brand-black border border-brand-border text-zinc-100 p-3 text-sm focus:border-brand-gold focus:outline-none">
      </div>
      <div>
        <label class="block text-xs uppercase tracking-widest text-brand-muted mb-2" for="message">Mensaje / Obra de interés</label>
        <textarea id="message" rows="4" required class="w-full bg-brand-black border border-brand-border text-zinc-100 p-3 text-sm focus:border-brand-gold focus:outline-none"></textarea>
      </div>
      <button type="submit" class="w-full py-3 bg-brand-gold text-black text-xs uppercase tracking-widest font-medium hover:bg-brand-goldHover transition-colors">
        Enviar Mensaje
      </button>
    </form>
  </section>

  <!-- FOOTER -->
  <footer class="py-8 border-t border-brand-border text-center text-xs text-brand-muted">
    <p>© MIRADAS — Nacho Pedrós. Todos los derechos reservados.</p>
  </footer>

  <!-- LIGHTBOX / MODAL OBRAS -->
  <div id="modal" class="fixed inset-0 bg-black/95 z-50 hidden flex items-center justify-center p-4">
    <button id="modal-close" class="absolute top-6 right-6 text-zinc-400 hover:text-white text-3xl focus:outline-none">&times;</button>
    
    <div class="max-w-4xl w-full grid grid-cols-1 md:grid-cols-2 gap-8 items-center max-h-[90vh] overflow-y-auto">
      <div class="flex justify-center">
        <img id="modal-img" src="" alt="" class="max-h-[70vh] object-contain border border-brand-border">
      </div>
      <div class="space-y-4 text-left p-4">
        <span id="modal-category" class="text-xs uppercase tracking-widest text-brand-gold"></span>
        <h3 id="modal-title" class="text-3xl font-serif font-light text-zinc-100"></h3>
        <p id="modal-details" class="text-sm text-brand-muted"></p>
        <p id="modal-desc" class="text-sm text-zinc-300 font-light leading-relaxed"></p>
        <div class="pt-4">
          <button id="modal-inquire" class="px-6 py-2 border border-brand-gold text-brand-gold hover:bg-brand-gold hover:text-black text-xs uppercase tracking-widest transition-colors">
            Consultar sobre esta obra
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- JAVASCRIPT -->
  <script>
    // LISTA DE OBRAS REALES
    const artworks = [
      { id: 1, title: 'Mirada Atenta', category: 'Retratos', technique: 'Carboncillo sobre papel', year: '2024', status: 'Disponible', image: 'image.png', desc: 'Estudio de mirada infantil velada con trazos expresivos y grafito.' },
      { id: 2, title: 'Serenidad en Penumbra', category: 'Claroscuro', technique: 'Carboncillo y tiza blanca', year: '2024', status: 'Disponible', image: 'image_2.png', desc: 'Retrato dramático enfocado en la elevación de la luz sobre el rostro.' },
      { id: 3, title: 'Reflexión Introspectiva', category: 'Retratos', technique: 'Grafito y pigmento', year: '2023', status: 'Disponible', image: 'image_3.png', desc: 'Figura en actitud contemplativa apoyando el rostro, rodeada de trazos gestuales.' },
      { id: 4, title: 'Abandono Suave', category: 'Retratos', technique: 'Carboncillo sobre papel', year: '2024', status: 'Disponible', image: 'image_4.png', desc: 'Gesto tenue de meditación y serenidad en tonos grises y negros profundos.' },
      { id: 5, title: 'El Gondolero en la Noche', category: 'Claroscuro', technique: 'Tiza blanca sobre fondo negro', year: '2023', status: 'Disponible', image: 'image_5.png', desc: 'Composición de contraste extremo que emerge directamente del fondo negro.' },
      { id: 6, title: 'Piedad en Sombras', category: 'Sacro & Expresión', technique: 'Carboncillo blanco sobre soporte oscuro', year: '2024', status: 'Disponible', image: 'image_6.png', desc: 'Figura devocional iluminada por un destello focal de luz.' },
      { id: 7, title: 'El Calvario Sustraído', category: 'Sacro & Expresión', technique: 'Técnica mixta sobre fondo negro', year: '2023', status: 'Disponible', image: 'image_7.png', desc: 'Estudio corporal en tensión a través de claroscuros extremos.' },
      { id: 8, title: 'Cristo en Penumbra', category: 'Sacro & Expresión', technique: 'Pigmento sobre lienzo negro', year: '2024', status: 'Disponible', image: 'image_8.png', desc: 'Figura sacra suspendida en la oscuridad más absoluta.' },
      { id: 9, title: 'Perfil de Silencio', category: 'Claroscuro', technique: 'Tiza blanca sobre papel negro', year: '2024', status: 'Disponible', image: 'image_9.png', desc: 'Sutil trazado de un perfil emergiendo desde el fondo oscuro.' },
      { id: 10, title: 'La Bailarina (El Cisne)', category: 'Claroscuro', technique: 'Carboncillo blanco sobre negro', year: '2024', status: 'Disponible', image: 'image_10.png', desc: 'Captura de movimiento y extensión de la figura en danza.' }
    ];

    // RENDERIZAR OBRAS EN EL GRID
    const grid = document.getElementById('artwork-grid');

    function renderArtworks(filter = 'all') {
      grid.innerHTML = '';
      const filtered = filter === 'all' ? artworks : artworks.filter(a => a.category === filter);

      filtered.forEach(art => {
        const card = document.createElement('div');
        card.className = 'artwork-card bg-brand-card border border-brand-border overflow-hidden cursor-pointer group transition-all duration-300 hover:border-brand-gold';
        card.innerHTML = `
          <div class="image-zoom-container relative aspect-[3/4] bg-zinc-900">
            <img src="${art.image}" alt="${art.title}" class="w-full h-full object-cover">
            <div class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
              <span class="text-xs uppercase tracking-widest text-brand-gold border border-brand-gold px-4 py-2 bg-black/60">Ver Detalle</span>
            </div>
          </div>
          <div class="p-5 space-y-1">
            <span class="text-[10px] uppercase tracking-widest text-brand-gold">${art.category}</span>
            <h3 class="text-xl font-serif text-zinc-100 font-light">${art.title}</h3>
            <p class="text-xs text-brand-muted">${art.technique} — ${art.year}</p>
          </div>
        `;
        card.addEventListener('click', () => openModal(art));
        grid.appendChild(card);
      });
    }

    renderArtworks();

    // FILTROS DE CATEGORÍA
    document.querySelectorAll('.filter-btn').forEach(btn => {
      btn.addEventListener('click', (e) => {
        document.querySelectorAll('.filter-btn').forEach(b => {
          b.classList.remove('bg-brand-gold', 'text-black');
          b.classList.add('text-brand-muted');
        });
        e.target.classList.add('bg-brand-gold', 'text-black');
        e.target.classList.remove('text-brand-muted');
        renderArtworks(e.target.dataset.filter);
      });
    });

    // MENÚ DESPLEGABLE DRAWER
    const menuBtn = document.getElementById('menu-btn');
    const closeBtn = document.getElementById('close-btn');
    const drawer = document.getElementById('menu-drawer');
    const overlay = document.getElementById('menu-overlay');

    function openMenu() {
      drawer.classList.remove('translate-x-full');
      overlay.classList.remove('pointer-events-none', 'opacity-0');
      overlay.classList.add('opacity-100');
    }

    function closeMenu() {
      drawer.classList.add('translate-x-full');
      overlay.classList.add('pointer-events-none', 'opacity-0');
      overlay.classList.remove('opacity-100');
    }

    menuBtn.addEventListener('click', openMenu);
    closeBtn.addEventListener('click', closeMenu);
    overlay.addEventListener('click', closeMenu);

    document.querySelectorAll('.menu-link').forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        closeMenu();
        const targetId = link.getAttribute('href');
        const targetElement = document.querySelector(targetId);
        if (targetElement) {
          const headerOffset = 80;
          const elementPosition = targetElement.getBoundingClientRect().top;
          const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
          window.scrollTo({ top: offsetPosition, behavior: 'smooth' });
        }
      });
    });

    // MODAL LIGHTBOX
    const modal = document.getElementById('modal');
    const modalImg = document.getElementById('modal-img');
    const modalTitle = document.getElementById('modal-title');
    const modalCategory = document.getElementById('modal-category');
    const modalDetails = document.getElementById('modal-details');
    const modalDesc = document.getElementById('modal-desc');
    const modalClose = document.getElementById('modal-close');
    const modalInquire = document.getElementById('modal-inquire');

    let currentArt = null;

    function openModal(art) {
      currentArt = art;
      modalImg.src = art.image;
      modalTitle.textContent = art.title;
      modalCategory.textContent = art.category;
      modalDetails.textContent = `${art.technique} | ${art.year} | ${art.status}`;
      modalDesc.textContent = art.desc;
      modal.classList.remove('hidden');
    }

    modalClose.addEventListener('click', () => modal.classList.add('hidden'));

    modalInquire.addEventListener('click', () => {
      modal.classList.add('hidden');
      const contactSection = document.getElementById('contacto');
      const messageField = document.getElementById('message');
      if (messageField && currentArt) {
        messageField.value = `Hola Nacho, me gustaría consultar la disponibilidad e información de la obra "${currentArt.title}".`;
      }
      contactSection.scrollIntoView({ behavior: 'smooth' });
    });
  </script>
</body>
</html>